{
  "private": true,
  "type": "module",
  "name": "autometrics-monorepo",
  "workspaces": [
    "packages/autometrics",
    "packages/autometrics-lib",
    "packages/autometrics-typescript-plugin",
    "packages/parcel-transformer-autometrics"
  ],
  "main": "index.js",
  "scripts": {
    "build:main": "npm run build --workspace=autometrics",
    "dev:main": "npm run dev --workspace=autometrics",
    "clean:main": "npm run clean --workspace=autometrics",
    "build:lib": "npm run build --workspace=@autometrics/autometrics",
    "dev:lib": "npm run dev --workspace=@autometrics/autometrics",
    "clean:lib": "npm run clean --workspace=@autometrics/autometrics",
    "build:typescript-plugin": "npm run build --workspace=@autometrics/typescript-plugin",
    "dev:typescript-plugin": "npm run dev --workspace=@autometrics/typescript-plugin",
    "clean:typescript-plugin": "npm run clean --workspace=@autometrics/typescript-plugin",
    "build:parcel-plugin": "npm run build --workspace=@autometrics/parcel-transformer-autometrics",
    "dev:parcel-plugin": "npm dev build --workspace=@autometrics/parcel-transformer-autometrics",
    "clean:parcel-plugin": "npm clean build --workspace=@autometrics/parcel-transformer-autometrics",
    "build": "npm run build --workspaces",
    "test": "vitest",
    "lint": "rome ci packages/**/src"
  },
  "devDependencies": {
    "rome": "^11.0.0",
    "vitest": "^0.28.4"
  }
}
