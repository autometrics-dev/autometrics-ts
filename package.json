{
	"private": true,
	"type": "module",
	"name": "autometrics-monorepo",
	"workspaces": [
		"packages/autometrics-docs",
		"packages/autometrics"
	],
	"main": "index.js",
	"scripts": {
		"dev:lib": "npm run dev --workspace=@autometrics/autometrics",
		"clean:lib": "npm run clean --workspace=@autometrics/autometrics",
		"dev:docs": "npm run dev --workspace=@autometrics/docs",
		"clean:docs": "npm run clean --workspace=@autometrics/docs",
		"test": "vitest"
	},
	"devDependencies": {
		"rome": "^11.0.0",
		"vitest": "^0.28.4"
	}
}
